
Development
======================

1. Run "pip install -e . " to install all the dependencies and add a pointer to the nwrsc code

3. Run "python3 bin/webserver" to run the web server


Installing
======================

1. Run "pip install . " to install in the current working python installation/virtual env

